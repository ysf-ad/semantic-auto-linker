---
tags:
  - semantic
  - playground
---

# Semantic Challenge Note

This note is meant to stress semantic matching rather than direct title overlap.
It talks about connectionist systems, dense [[Semantic Playground/Meaning As Coordinates|meaning vectors]], angle-based similarity, on-device model execution, and a pluggable backend layer without naming the exact destination notes directly. 

The broader goal is to turn a scattered writing system into a durable web of long-lived idea pages that keep getting refined instead of being discarded. I am now testing another keyword

## Protected content checks

The heading above should still stay untouched if semantic linking is turned on later.

Inline code like `run the model locally` should not get linked.

```ts
const phrase = "dense meaning vectors";
```

principle component analysis