# Selection Playground

Before the selection there is machine learning text that should remain unchanged.

Select only this [[Hobbies/Tennis/Match Preparation|paragraph before running]] the selection command.
ML is written here as an alias.
Graph theory is written here as a direct title.

After the selection there is another machine learning mention that should not be touched if your selection excludes it.

