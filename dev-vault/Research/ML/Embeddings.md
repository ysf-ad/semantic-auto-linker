---
aliases:
  - Vector Embeddings
tags:
  - ml
  - research
---

# Embeddings

Embeddings convert text into vectors that can be compared with cosine similarity.
The plugin does not use embeddings yet, but future semantic mode may use local inference and reranking.
This note references cosine similarity, local inference, and semantic search.
