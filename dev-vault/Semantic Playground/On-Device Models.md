---
tags:
  - semantic
  - local-ai
---

# On-Device Models

Instead of calling a hosted endpoint, some workflows run the model directly on the laptop.
That should land near the notes about local execution and also touch the architectural note about keeping the backend swappable.

The note intentionally avoids the exact phrases used in the target titles.

