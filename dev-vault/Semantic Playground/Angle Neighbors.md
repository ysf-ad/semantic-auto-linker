---
tags:
  - semantic
  - retrieval
---

# Angle Neighbors

This note talks about comparing points by the angle between them and retrieving nearby items in latent space.
It should feel semantically related to vector comparison and numeric text representations, even though it avoids the exact target titles.

It is also a good place to test whether the graph later pulls retrieval concepts closer to embedding-heavy notes.

